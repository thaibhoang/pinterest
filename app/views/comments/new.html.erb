<%= turbo_frame_tag(@turbo_frame_id) do %>
  <%= render partial: "comments/form", 
             locals: { pin: @pin, 
                       comment: @comment, 
                       cancel_form: @cancel_form } %>
<% end %>
