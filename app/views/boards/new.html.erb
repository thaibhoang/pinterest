<div class="middle-container">
  <div>
    <h1>New board</h1>
  
    <%= render partial: "boards/form", 
      locals: {board: @board, 
               pin_id: defined?(@pin_id) ? @pin_id : nil, 
               in_saved_pin_mode: @in_saved_pin_mode,
               cancel_form: @cancel_form} %>
  
    <br>
  
    <div>
      <%= link_to "Back to boards", user_boards_path(current_user) %>
    </div>
  </div>
</div>
