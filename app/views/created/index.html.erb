<%= render partial: "shared/profile", locals: { user: @user } %>
<hr>
<div class="md big m1">All Pins Created</div>
<%= turbo_frame_tag "content" do %>
  <div class="container">
    <% cache @pins do %>
      <% @pins.each do |pin| %>
        <% cache pin do %>
          <%= render partial: "shared/pin", locals: {pin: pin, edit_pin: true } %>
        <% end %>
      <% end %>
    <% end %>
  </div>

  <% if @user == current_user && @user.pins.count == 0 %>
    <div class="m1 md">
      <div>You haven't created any pin yet.</div>
      <div><%= link_to "Create One", new_pin_path, data: {turbo_frame: "_top"} %></div>
    </div>
  <% end %>
<% end %>


