<div class="bottom-right">
  <%= link_to edit_pin_path(pin, in_frame_mode: true), class: "white bold", data: {turbo_frame: "#{dom_id(pin)}_edit_layout"} do %>
    <span>Edit</span>
    <i class="fa-solid fa-pen"></i>
  <% end %>
  <%= turbo_frame_tag [dom_id(pin), "edit_layout"] %>
</div>