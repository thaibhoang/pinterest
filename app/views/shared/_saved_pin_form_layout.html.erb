<%= form_with model: [pin, pin.saved_pins.build] do |f| %>
  <%= f.submit "Save" %>
<% end %>
<%= link_to "Choose board" ,new_pin_saved_pin_path(pin), data: { turbo_frame: "#{dom_id(pin)}_new_saved_pin" } %>
<%= turbo_frame_tag("#{dom_id(pin)}_new_saved_pin") %>
