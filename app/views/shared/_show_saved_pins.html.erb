<div class="container">
  <% cache saved_pins do %>
    <% saved_pins.each do |saved_pin| %>
      <% cache saved_pin do %>
        <%= render partial: "shared/pin", locals: {pin: saved_pin.pin } %>
      <% end %>
    <% end %>
  <% end %>
</div>