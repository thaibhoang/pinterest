<%= form_with(model: [pin, note]) do |form| %>
  <% if note.errors.any? %>
    <div style="color: red">
      <div><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</div>

      <ul>
        <% note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, "Note content", style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
